[package]
name = "lean-incremental-merkle-tree"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
poseidon = { path = "../poseidon" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }